export function makeDate(input){
  if(isNaN(input)){
    input = "Invalid Input";
    } else{
      input = parseInt(input)
    }return input;
  }

// class dateCalc {
//   constructor (month, day, year){
//     this.month = month,
//     this.day = day,
//     this.year = year
//   }
// }
